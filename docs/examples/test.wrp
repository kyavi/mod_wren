<!DOCTYPE html>
<html>
<head>
	<title>Wren Test</title>
	<meta charset="utf-8"></meta>
</head>
<body>
	<h1>Wren Test</h1>
	<span>
		Post test:
	</span>
	<form method="post">
		<input type="text" placeholder="one" name="one">
		<input type="text" placeholder="two" name="two">
		<input type="text" placeholder="three" name="three">
		<input type="checkbox" name="checkbox">
		<input type="submit">
	</form>
	<div><?wren
var post_params = Web.parsePost()

if(post_params is Map) {
	System.write("<div><b>Returned values:</b></div>")

	for(x in post_params.keys) {
		System.write("<div><b>%(x):</b> %(post_params[x])</div>")
	}
} ?>
	</div>
	<p>
		Headers from apache:
	</p><?wren
var env = Web.getEnv()
System.write("") /* Bug: This should not be necessary. */

for(x in env.keys) {
   System.write("<div><b>%(x):</b> %(env[x])</div>")
} ?>
	<p>
		The list of escape characters from Wren's docs:
	</p>
	<pre>
"\0" // The NUL byte: 0.
"\"" // A double quote character.
"\\" // A backslash.
"\%" // A percent sign.
"\a" // Alarm beep. (Who uses this?)
"\b" // Backspace.
"\f" // Formfeed.
"\n" // Newline.
"\r" // Carriage return.
"\t" // Tab.
"\v" // Vertical tab.
	</pre>
	<p>
		Expression block can be written like so:
	</p>
	<pre>
&lt;%= "This " + "is " + "an " + "expression " + "block " + "of " +
	"concatenated " + "strings." %&gt;
	</pre>
	<p>
		Resulting in the output:
		<%= "This " + "is " + "an " + "expression " + "block " + "of " +
			"concatenated " + "strings." %>
	</p>
</body>
