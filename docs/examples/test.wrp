<!DOCTYPE html>
<html>
<head>
	<title>Wren Test</title>
	<meta charset="utf-8"></meta>
</head>
<body>
	<h1>Wren Test</h1>
	<p>
		Printing a list of numbers \0\a from the server:
	</p><?wren
for(x in Web.getTestList()) {
	System.write("<div>%(x)</div>")
} ?>
	<p>
		And a few more times, with single characters in between the Wren tags.
	</p><?wren
for(x in Web.getTestList()) {
	System.write("<div>%(x)</div>")
} ?>"<?wren
for(x in Web.getTestList()) {
	System.write("<div>%(x)</div>")
} ?>%<?wren
for(x in Web.getTestList()) {
	System.write("<div>%(x)</div>")
} ?>
	<p>
		And the list of escape characters from Wren's docs:
	</p>
	<pre>
"\0" // The NUL byte: 0.
"\"" // A double quote character.
"\\" // A backslash.
"\%" // A percent sign.
"\a" // Alarm beep. (Who uses this?)
"\b" // Backspace.
"\f" // Formfeed.
"\n" // Newline.
"\r" // Carriage return.
"\t" // Tab.
"\v" // Vertical tab.
	</pre>
</body>
